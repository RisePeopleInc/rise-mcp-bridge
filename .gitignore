# build output
/dist/

# per-user runtime secrets (should never be near this repo, but belt-and-suspenders)
config.json
token.json
client_id

# go
*.test
*.out


#Ignore vscode AI rules
.github/instructions/codacy.instructions.md

# local build artifacts (binaries built at repo root by `go build .`)
/rise-mcp-bridge
/rise-mcp-bridge.exe

# Cowork/agent worktree state
.claude/
resource_windows_amd64.syso
